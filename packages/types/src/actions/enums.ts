/** Permission level enums */
export const enum PermissionLevel {
    OFF = 0,
    COPILOT = 1,
    AUTOPILOT = 2,
}

/** Type of diff range */
export const enum DiffRangeType {
    Added,
    Modified,
    Removed,
}
